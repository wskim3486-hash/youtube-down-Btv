# 회사 구성원 전용 영상 다운로드 웹 서비스 요구사항

## 1. 프로젝트 목표

Windows와 macOS에서 동일하게 사용할 수 있는 회사 구성원 전용 영상 다운로드 웹 서비스를 구현한다.

사용자가 YouTube, Instagram 등 SNS 영상과 국회 영상회의록시스템, 지방자치단체·구청·시의회·구의회 등 공개 영상 페이지의 URL을 입력하면 해당 영상을 자동으로 분석한다. 사이트가 실제로 제공하는 화질 중 원하는 화질을 선택해 MP4로 다운로드하거나 오디오만 MP3로 추출할 수 있어야 한다.

사이트별 구조 차이를 고려해 Provider/Adapter 방식으로 설계하며, 국회 영상회의록시스템처럼 HLS(m3u8) 기반으로 재생되는 사이트도 지원한다.

특정 운영체제에 종속되지 않도록 웹 애플리케이션을 우선 개발한다. Windows의 Chrome/Edge와 macOS의 Chrome/Safari에서 동일하게 사용할 수 있어야 한다.

DRM 해제, 로그인·인증 우회, 유료·비공개 콘텐츠 접근 우회는 지원하지 않는다. 공개적으로 재생 가능한 영상 또는 회사가 정당한 이용 권한을 가진 콘텐츠만 대상으로 한다.

비개발자인 사내 구성원도 URL 하나만 붙여넣으면 쉽게 사용할 수 있는 단순한 UI를 목표로 한다.

## 2. 핵심 사용자 흐름

1. 사용자가 영상 URL을 입력한다.
2. URL을 분석한다.
3. 사이트와 영상 정보를 확인한다.
4. 사이트에서 제공하는 화질을 표시한다.
5. MP4 또는 MP3를 선택한다.
6. 다운로드를 실행한다.
7. 진행 상태를 표시한다.
8. 완료된 파일을 다운로드한다.

## 3. 지원 대상 예시

- YouTube
- Instagram
- 국회 영상회의록시스템
- 지방자치단체 홈페이지
- 구청 홈페이지
- 시의회
- 구의회
- 기타 공개 영상 페이지

## 4. 영상 분석 결과

가능한 범위에서 다음 정보를 표시한다.

- 사이트명
- 영상 제목
- 썸네일
- 영상 길이
- 사용 가능한 해상도
- 비디오 포맷
- 오디오 포맷
- FPS
- 예상 파일 크기
- MP3 추출 가능 여부

## 5. 화질 처리

- 사이트가 실제 제공하는 화질만 표시한다.
- 지원 화질의 예는 2160p, 1440p, 1080p, 720p, 480p, 360p, 240p이다.
- 사이트에서 제공하지 않는 화질을 인위적으로 업스케일하지 않는다.
- 가능하면 최고 화질을 기본 선택값으로 한다.

## 6. 다운로드 형식과 미디어 처리

- 영상 출력 형식: MP4
- 오디오 출력 형식: MP3

필요하면 FFmpeg를 이용해 다음 작업을 처리한다.

- 비디오와 오디오 병합
- HLS를 MP4로 변환
- 오디오를 MP3로 변환
- 컨테이너 변환

Final Cut Pro와 Premiere Pro에서 사용할 수 있는 일반적인 결과물을 우선한다.

## 7. 크로스플랫폼 요구사항

- Windows와 macOS에서 모두 사용할 수 있어야 한다.
- 특정 OS에 종속된 구조로 만들지 않는다.
- 웹 애플리케이션을 우선한다.
- 사용자는 별도 개발 환경이나 FFmpeg, yt-dlp 등을 직접 설치하지 않고 브라우저만으로 사용할 수 있어야 한다.
- Windows용과 macOS용으로 별도 코드베이스를 만들지 않는다.
- 영상 분석, 다운로드, 변환, 병합은 가능한 한 서버 백엔드에서 처리하여 사용자 OS에 의존하지 않게 한다.
- 향후 필요하면 Electron 또는 Tauri를 이용해 데스크톱 앱으로 패키징할 수 있는 확장성을 고려한다.

우선 지원 브라우저:

- Windows: Chrome, Edge
- macOS: Chrome, Safari

## 8. Provider / Adapter 구조

사이트마다 영상 제공 방식이 다르므로 하나의 거대한 다운로드 로직으로 만들지 않는다.

예시 구조:

```text
providers/
├─ youtube/
├─ instagram/
├─ assembly/
├─ council/
└─ generic/
```

공통 인터페이스 예:

- `canHandle(url)`
- `analyze(url)`
- `getFormats(url)`
- `download(url, format)`
- `extractAudio(url)`

새로운 사이트를 추가할 때 기존 핵심 코드를 크게 수정하지 않고 새 Provider만 추가할 수 있어야 한다.

## 9. Engine 구조

외부 다운로드·변환 도구는 핵심 코드와 분리한다.

예시 구조:

```text
engines/
├─ ytdlp/
├─ ffmpeg/
└─ hls/
```

의존 방향은 다음과 같다.

```text
Core → Provider → Engine
```

특정 도구가 고장 나거나 교체되어도 서비스 전체를 다시 설계하지 않도록 한다.

## 10. yt-dlp 운용 전략

yt-dlp는 YouTube, Instagram 등 사이트 구조 변경에 따라 갑자기 작동하지 않을 수 있다는 점을 전제로 설계한다.

다음 기능을 고려한다.

- 현재 yt-dlp 버전 확인
- 마지막 정상 동작 버전 기록
- 업데이트 가능 여부 확인
- 관리자 수동 업데이트
- 업데이트 후 자동 테스트
- 실패 시 이전 정상 버전으로 롤백
- 사이트별 yt-dlp 동작 상태 기록
- extractor 오류 로그 수집

외부 오픈소스의 최신 버전을 자동으로 즉시 운영 환경에 적용하지 않는다.

권장 배포 흐름:

```text
현재 운영 버전
→ 새 버전 발견
→ 테스트 환경
→ 대표 URL 자동 테스트
→ 성공 시 운영 반영
→ 실패 시 이전 정상 버전 유지
```

향후 관리자 화면에서 다음 정보를 확인할 수 있게 고려한다.

- 현재 yt-dlp 버전
- 최신 버전
- 업데이트 가능 여부
- 마지막 업데이트
- 마지막 정상 테스트
- 사이트별 상태
- 오류 로그
- 롤백 가능 버전

## 11. 오픈소스 활용 전략

모든 기능을 처음부터 직접 구현하지 않는다. GitHub 등에서 안정적으로 유지되는 검증된 오픈소스가 있다면 적극 활용한다.

검토 대상 예시:

- yt-dlp
- FFmpeg
- HLS 관련 라이브러리
- 미디어 메타데이터 분석 라이브러리
- 사이트별 extractor/parser
- 다운로드 Queue 관련 라이브러리

단, 사용자가 GitHub 저장소 URL을 입력하면 자동으로 clone한 뒤 즉시 실행하는 구조는 만들지 않는다.

오픈소스 도입 절차:

1. 라이선스 확인
2. 유지보수 상태 확인
3. 최근 업데이트 여부 확인
4. 보안 위험 검토
5. 필요한 기능만 모듈화
6. 버전 고정
7. 테스트
8. 운영 반영

오픈소스는 Provider 또는 Engine 형태로 래핑하여 핵심 코드와 분리한다.

## 12. 국회 영상회의록시스템

국회 영상회의록시스템은 `AssemblyProvider`로 별도 구현하는 방향을 검토한다.

현재 확인된 구조:

- Video.js 사용
- Hls.js 사용
- m3u8 HLS 스트림 사용
- `vsrc`가 플레이어 소스로 전달됨
- `hls_urls` 배열을 이용한 화질 변경 구조
- 240p / 480p / 720p 화질 구조 확인

페이지 URL 예:

```text
https://w3.assembly.go.kr/main/player.do?...
```

사용자가 이러한 URL을 입력하면 페이지와 공개 네트워크 구조를 분석해 공개적으로 재생되는 HLS 스트림을 식별할 수 있는 구조를 만든다.

- 사이트가 실제 제공하는 화질만 사용자에게 보여준다.
- 특정 영상 URL을 하드코딩하지 않는다.
- 다른 국회 영상에도 적용되는 규칙 기반 Provider로 설계한다.

## 13. Generic Provider

전용 Provider가 없는 사이트는 `GenericProvider`가 처리한다.

공개적으로 접근 가능한 다음 리소스를 탐지할 수 있는지 검토한다.

- Direct MP4
- HTML5 video source
- m3u8
- HLS master playlist
- DASH manifest
- Video.js source
- 공개 API media URL

보호 기능이나 인증을 우회하지 않는다.

## 14. 다운로드 진행 상태와 오류 분류

사용자에게 다음과 같은 처리 상태를 보여준다.

```text
URL 확인 중
→ 사이트 분석 중
→ 영상 정보 확인
→ 영상 다운로드 중
→ 영상/음성 병합 중
→ 파일 생성 중
→ 다운로드 준비 완료
```

가능하면 진행률을 백분율로 표시한다.

오류 발생 시 가능한 범위에서 다음 원인을 구분한다.

- 지원하지 않는 사이트
- 영상 URL을 찾을 수 없음
- 접근 권한 필요
- DRM 콘텐츠
- 영상 삭제됨
- 네트워크 오류
- yt-dlp extractor 오류
- FFmpeg 변환 오류
- 사이트 구조 변경 가능성

## 15. 서비스 상태 점검

외부 사이트 구조 변경에 영향을 받으므로 지원 사이트 상태 점검 기능을 고려한다.

상태 표시 예:

- YouTube: 정상
- Instagram: 점검 필요
- 국회: 정상
- 특정 의회: 실패

각 Provider별 대표 테스트 URL을 등록하고 정기적으로 정상 분석 여부를 점검할 수 있는 구조를 고려한다.

문제가 생겼을 때 다음 중 어디에 가까운지 판단할 수 있도록 로그를 남긴다.

- Provider 문제
- yt-dlp 문제
- 사이트 구조 변경
- 네트워크 문제
- FFmpeg 문제

## 16. 사용자 UI

사용자는 개발자가 아니므로 첫 화면은 최대한 단순하게 구성한다.

기본 화면 예:

```text
[ 영상 URL을 붙여넣으세요 ]

[ 분석하기 ]
```

분석 후 표시 항목:

- 썸네일
- 영상 제목
- 영상 길이
- 화질 선택
- MP4 / MP3 선택
- 다운로드 버튼

복잡한 개발자 정보는 기본 화면에서 숨기고 필요한 경우 상세 정보에서만 보여준다.

## 17. 관리자 기능

향후 관리자 화면에서 다음을 관리할 수 있는 구조를 고려한다.

- 지원 사이트 목록
- Provider 활성/비활성
- Engine 상태
- yt-dlp 버전
- FFmpeg 버전
- 업데이트 상태
- 사이트별 테스트 결과
- 오류 로그
- 다운로드 실패 통계
- Provider 버전
- 오픈소스 라이선스 정보

## 18. 사내 서비스 확장

향후 다음 기능을 추가할 수 있도록 구조를 고려한다.

- 사내 로그인
- 사용자별 다운로드 기록
- 여러 URL 일괄 다운로드
- 다운로드 Queue
- ZIP 다운로드
- 다운로드 완료 알림
- 관리자 화면
- 지원 사이트 현황
- 사이트별 오류 로그
- 다운로드 통계

## 19. 보안 및 지원 제한

다음 기능은 구현하지 않는다.

- DRM 해제
- 로그인 우회
- 인증 우회
- 유료 콘텐츠 우회
- 비공개 콘텐츠 접근 우회
- 암호키 추출
- 접근 권한 회피

공개적으로 재생 가능한 영상 또는 회사가 정당한 이용 권한을 가진 콘텐츠만 처리한다.

## 20. MVP 구현 순서

처음부터 모든 기능을 구현하지 않는다. 각 단계가 정상 작동하는지 테스트한 뒤 다음 단계로 진행한다.

1. 프로젝트 기본 구조
2. URL 입력 UI
3. 분석 API
4. Provider 인터페이스
5. Engine 인터페이스
6. Generic Provider
7. 테스트 가능한 사이트 Provider 1개
8. MP4 다운로드
9. MP3 추출
10. 다운로드 진행 상태
11. AssemblyProvider
12. yt-dlp 상태/버전 관리 기본 구조
13. 추가 사이트 Provider
14. 관리자용 상태 확인 기능

## 21. 기술 설계 원칙

- 소규모 사내 서비스에 적합하고 유지보수하기 쉬운 구조를 우선한다.
- 불필요하게 복잡한 엔터프라이즈 구조는 만들지 않는다.
- yt-dlp, FFmpeg, HLS 관련 라이브러리, Video.js/Hls.js 분석, 다운로드 Queue, 백그라운드 작업 처리 도구의 사용 여부를 검토한다.
- 긴 영상 다운로드와 FFmpeg 변환은 일반적인 짧은 HTTP 요청으로 처리하기 어려울 수 있으므로 비동기 작업 Queue가 필요한지 판단한다.

## 22. Git 작업 원칙

- 현재 프로젝트의 Git 상태를 먼저 확인한다.
- 기존 파일이 있다면 불필요하게 삭제하거나 전체 구조를 바꾸지 않는다.
- 본격 구현 전에 현재 초기 상태를 커밋하기 좋은 지점으로 알려준다.
- 큰 구조 변경이 필요하면 feature branch 또는 worktree 사용을 먼저 제안한다.
- 주요 기능 하나가 정상 작동할 때마다 커밋하기 좋은 지점을 알려준다.
- 관련 없는 파일은 수정하지 않는다.

수정 후 항상 다음 내용을 요약한다.

- 수정한 파일 목록
- 주요 변경 내용
- 테스트 결과
- 현재 문제점
- 다음 작업
- 커밋 권장 여부
